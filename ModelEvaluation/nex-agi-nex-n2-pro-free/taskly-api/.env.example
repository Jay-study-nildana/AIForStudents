# Taskly API Environment Variables
# Copy this file to .env and replace placeholder values before running locally.

PORT=3000
NODE_ENV=development
DB_PATH=./data/taskly.sqlite

# Use a long, random secret in real environments.
JWT_SECRET=replace_with_strong_secret
# Optional CORS origin for local frontend/browser testing.
CORS_ORIGIN=http://localhost:3000
